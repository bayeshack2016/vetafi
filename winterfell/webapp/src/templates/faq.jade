#faq-view(ng-controller="faqCtrl")
  div.search-wrapper
    div.search-icon
    input.search-bar(type="text" ng-model="searchText" ng-change="onSearchChange()" placeholder="Search frequently asked questions")
  div.section-card(ng-repeat="section in sections | filter:searchText")
    div.section-title {{section.title}}
    div.section-divider
    div.section-content {{section.content}}
