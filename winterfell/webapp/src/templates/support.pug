#vfi-support-view
  div.support-wrapper
    h3 Have questions or comments?
    h3 Send us an email

    h5 If you haven't already, check our #[a(ui-sref="root.faq") FAQ page] to see if your question is answered there.

    form(action="mailto:support@vetafi.org", method="post", enctype="text/plain")
      div.section
        div.section-label Name:
        input.vfi-default(ng-model="userName", type="text", name="name", placeholder="Enter your name here")

      div.section
        div.section-label Email:
        input.vfi-default(ng-model="userEmail", type="text", name="email", placeholder="Enter your email here")

      div.section
        div.section-label Comments:
        textarea(rows="4", cols="60", name="comment")

      button
        input.submit(type="submit" value="Send")
vfi-footer
